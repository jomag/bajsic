const chai = require('chai');
const expect = chai.expect;

//import { ExprType, ValueType, evalExpr, addOp, constInt, subOp } from './expr';

/*
describe('Expression', () => {
  it('evalutes single leaf tree', () => {
    const e = constInt(42);
    const v = evalExpr(e);
    expect(v).to.deep.equal({ type: ValueType.INT, value: 42 });
  });

  it('evaluates nested tree', () => {
    const e = addOp(
      subOp(constInt(5), constInt(3)),
      addOp(constInt(8), constInt(1))
    );
    const v = evalExpr(e);
    expect(v).to.deep.equal({ type: ValueType.INT, value: 11 });
  });
});
*/
